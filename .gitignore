node_modules/
dist/
.env
.env.*
!.env.example
*.log
.DS_Store
.claude/

# Generated by scripts/generate-version.mjs (postinstall) from package.json's
# version — a bun --compile binary can't read package.json at runtime, so
# this gets baked in at build time instead. Never hand-edit or commit it.
src/version.ts
